# Database Configuration
# Pi PostgreSQL runs on port 5555 (not default 5432)
# Using TCP/IP on 127.0.0.1 for password authentication
DB_HOST=127.0.0.1
DB_PORT=5555
DB_USER=activityhub
DB_PASS=pubgames
DB_NAME=activity_hub

# Server Configuration
PORT=3001
