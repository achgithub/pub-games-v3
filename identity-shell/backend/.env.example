# Database Configuration
# Use Unix socket by default (faster, more secure for local connections)
# DB_HOST=/var/run/postgresql
# Or use TCP/IP for remote connections
# DB_HOST=192.168.1.100
DB_PORT=5555
DB_USER=pubgames
DB_PASS=pubgames
DB_NAME=pubgames

# Server Configuration
PORT=3001
